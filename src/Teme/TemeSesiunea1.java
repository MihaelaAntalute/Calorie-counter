package Teme;

public class TemeSesiunea1 {
    public static void main(String[] args) {
      // variabile
        // exercitiul 1: scrie un program care sa interschimbe valorile a doua variabile de tip int;
    /*    int a = 10;
        int b = 20;
        int temp = a;
        a = b;
        b = temp;
        System.out.println(a);
        System.out.println(b);


// exercitiul 2: scrie un program care sa calculeze media aritmetica a 3 variabile de tip int;

        double a3 = 4;
        double b3 = 3;
        double c3 = 1;
        System.out.println((a3 + b3 + c3) / 3);


        //exercitiul 3: scrie un program care converteste din grade Fahrenheit in grade Celsius;

        int temperature = 95;
        double tempF = 1.8;
        double tempC = 1;
        System.out.println((temperature - 32) / tempF);

        // conditional

        // exercitiul 1: avand la dispozitie doua variabile care stocheaza doua numere,
        // afiseaza-l la consola pe cel mai mare dintre ele;
        int a1 = 13;
        int b1 = 14;
        if (b1 > a1) {
            System.out.println("numarul mai mare este " + b1);
        } else {
            System.out.println("numarul mai mic este " + a1);
        }

        // exercitiul 2: avand la dispozitie o variabila care stocheaza un numar,
        // afiseaza in consola daca numarul este par sau impar;

        int a2 = 12;
        if (a2 % 2 == 0) {
            System.out.println("Numarul este par");
        } else {
            System.out.println("Numarul este impar");
        }


        //exercitiul 3:Daca previziunea pentru vreme este “rainy” sau previziunea este “snowing”
        // si viteaza vantului este mai mare decat 30
        // afiseaza in consola mesajul “Ramai in casa, este periculos afara”
        //Altfel, afiseaza mesajul: “S-ar putea sa fie frumos afara”
        //se va afisa in consola mesajul "Ramai in casa, este periculos afara"

        String currentForcast = "rainy";
        String currentForcast2 = "snowing";
        int currentWindSpeed = 40;
        if (currentWindSpeed > 30) {
            System.out.println("Ramai in casa,este periculos afara");
        } else {
            System.out.println("S-ar putea sa fie frumos afara");
        }

        // exercitiul 4:Avand la dispozitie o variabila care stocheaza un numar,
        // afiseaza in consola “fizz” daca numarul este multimplu de 3,
        // “buzz” daca numarul este multiplu de 5;
        // “fizzbuzz” daca numarul este divizibil atat cu 3 cat si cu 5
//se va afisa in consola "fizzbuzz", pentru ca 15 se imparte exact atat la 5, cat si la 3
//iar pentru  int = 9, se va afisa in consola "fizz", pentru ca 9 se imparte exact doar la 3

        int number = 15;
        if (15 / 3 == 0) {
            System.out.println("fizz");
        } else if (15 / 5 == 0) {
            System.out.println("buzz");
        } else if (15 % 3 == 0 && 15 % 5 == 0) {
            System.out.println("fittbuzz");
        }

        int number2 = 9;
        if (9 % 3 == 0) {
            System.out.println("fizz");
        } else {
            System.out.println("buzz");
        }

        //exercitiul 5:Avand 3 variabile care contin numere, scrie un program care sa printeze in consola:
        //“toate numerele sunt egale”, daca toate numerele sunt egale
        //“toate numerele sunt diferite”, daca toate numerele sunt diferite
        //“cel putin doua sunt egale”, daca oricare doua numere dintre cele trei sunt egale
        //se va afisa in consola: "cel putin doua sunt egale"

        int x = 3;
        int y = 3;
        int z = 4;
        if (x == y && y == z) {
            System.out.println("toate numerele sunt egale");
        } else if (x != y && y != z) {
            System.out.println("toate numerele sunt diferite");
        } else {
            System.out.println("cel putin doua sunt egale");
        }

        //exercitiul 6:Avand la dispozitie o variabila care stocheaza un an, afiseaza la consola daca anul este bisect sau nu.
        // Un an este bisect daca este divizibil cu 400 sau cu 4 si in acelasi timp nu este divizibil cu 100
        //se va afisa in consola: "anul 2020 este bisect"

        int year = 2020;
        if ((year % 4 == 0) && (year % 100 != 0)) {
            System.out.println("anul 2020 este bisect");
        } else {
            System.out.println("anul 2020 nu este bisect");
        }

        //exercitiul 7: Avand la dispozitie 2 variabile, guess si answer, creeaza un joc de ghicit
        //daca raspunsul este mai mic decat solutia (adica valoarea variabilei guess, afiseaza “nu ai ghicit, numarul este prea mic”
        //daca raspunsul este mai mare decat solutia, afiseaza “nu ai ghicit, numarul este prea mare”
        //daca raspunsul este egal cu solutia, afiseaza “ai ghicit”
        //se va afisa in consola: "nu ai ghicit, numarul este prea mare"

        int guess = 7;
        int answer = 8;
        if (answer < guess) {
            System.out.println("nu ai ghicit,numarul este prea mic");
        } else if (answer > guess) {
            System.out.println("nu ai ghicit,numarul este prea mare");
        } else {
            System.out.println("ai ghicit");
        }

     */
    }
}