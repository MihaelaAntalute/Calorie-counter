package Cursuri;

import java.util.Scanner;

public class MetodeCurs4 {
    public static void main(String[] args) {
        // cum verificam daca un numar este prim cu metode
        //  Scanner console = new Scanner(System.in);

        //int x;
        //System.out.println("introdu numar");
        //x = console.nextInt();
        //int number = console.nextInt();
      /*   int[]numbers= {5, 9, 11, 6, 4, 8};
        for(int i = 0; i < numbers.length; i++){
        if (isPrime(numbers[i])) {
            System.out.println("Numarul " + numbers[i] + " este prim");
        } else {
            System.out.println("Numarul " + numbers[i] + " nu este prim");
        }

        //vrem sa luam rezultatul boolean si sa il punem in isPrime1

    }

    public static boolean isPrime(int number) {
        boolean isPrime = true;
        for (int i = 2; i <= number / 2; i++) {
            if (number % i == 0) {
                isPrime = false;
                //    System.out.println(number + " nu este prim");
                break;
            }
        }

        /*if (isPrime) {
            System.out.println(number + " este prim");
        }

        return isPrime;
        */
        //}

    /*int numberOfProducts;
        public static void putProductsInCar(int numberOfProducts){
      while(numberOfProducts > 0){
          System.out.println("put next product in shooping cart");
          numbersOfProducts --;
      }

    }
}


     */
    }
}