package Cursuri;

public class ArrayCurs3 {

    public static void main(String[] args) {
        //exercitiul 1
        //Scrie un program care sa caute un numar de telefon intr-o lista de numere
        //programul va printa"numarul a fost gasit"
        //"numarul nu a fost gasit"

     /*   int[] numbers = {3, 14, 27, 51, 99};
        System.out.println(numbers[2]);
        System.out.println(numbers.length);

        //schimba valoarea lui [3]
        numbers[3] = 52;
        System.out.println(numbers[3]);

        //afiseaza fiecare element din array
        System.out.println("afisarea elementelor din array");
        for (int i = 0; i < numbers.length; i++) {
            System.out.println(numbers[i]);
        }

        System.out.println("cautarea numarului in array");
        int searchNumber = 3;
        //mergem prin fiecare numar
        //verificam daca este egal
        boolean isFound = false;
        for (int i = 0; i < numbers.length; i++) {
            System.out.println(numbers[i]);
            if (numbers[i] == searchNumber) {
                System.out.println("numarul a fost gasit");
                isFound = true;
                i = numbers.length;
                break;
            }
        }
        if (isFound == false) {
            System.out.println("numarul nu a fost gasit");
        }


      */
        //exercitiul3
        //Scrie un program care sa afiseze pretul mediu pe metru patrat,
        // dintr-o lista de preturi ale unor proprietati imobiliare:
        //De exempu, pentru int[] prices = {1, 7, 3, 10, 9},
        // se va afisa in consola valoarea 6;

    /*        int[]prices = {1, 7, 3, 10, 9};
            int sum = 0;
            int averagePrice = 1;
            System.out.println("pretul mediu pe metrul patrat");
            for (int i = 0; i < prices.length; i++){
               sum = sum + prices[i];
            }
            averagePrice = sum / prices.length;
            System.out.println(averagePrice);

     */
    }
}